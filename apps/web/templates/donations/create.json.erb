<%=
    {
        status: donation.id.nil? ? 'error' : 'success' ,
        donation: {
            name: donation.name,
            timestamp: donation.id.nil? ? 0 : donation.created_at.to_time.to_i
        }
    }.to_json

%>